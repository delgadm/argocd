apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shoehub
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: shoehub
  endpoints:
    - port: metrics
      path: /metrics
      interval: 5s
