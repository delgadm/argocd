# placeholder override to prove the values-file pipeline end-to-end
grafana:
  enabled: true
  admin:
    existingSecret: grafana-admin-credentials
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - grafana.local
    paths:
      - /

prometheus:
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - prometheus.local
    paths:
      - /
